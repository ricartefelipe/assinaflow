# Portal AssinaFlow

Interface do assinante (React + Vite + TypeScript).

## Desenvolvimento

Com a API em `http://localhost:8080`:

```bash
npm install
npm run dev
```

Abra `http://localhost:5173`.
